version: 2.1

jobs:
  build:
    docker:
      - image: node:10-alpine

    steps:
      - checkout
      - run:
          name: Install Node deps
          command: npm install
      - run:
          name: Build app
          command: npm run lint

  test:
    docker:
      - image: node:10-alpine

      steps:
         - checkout
         - run:
            name: Install Node deps
            command: npm install
         - run:
            name: Run  test
            command: npm run lint


  # analyze:

workflows:
  default:
    jobs:
      - build
      - test:
          requires:
            - build
      # - analyze:
      #     requires:
      #     - test
